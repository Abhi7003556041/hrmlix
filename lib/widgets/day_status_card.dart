import 'package:flutter/material.dart';

class DayStatusCard extends StatelessWidget {
  const DayStatusCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Row(
        children: [],
      ),
    );
  }
}
